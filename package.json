{
  "name": "matcode",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "server": "bun run --filter @matcode/server dev",
    "cli": "bun run --cwd apps/cli dev",
    "check": "fallow dead-code && fallow dupes"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "fallow": "^2.64.0"
  }
}
